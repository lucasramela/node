
    // function sumar( a, b ) {

    //     return a + b;

    // }

    // Cuando solamente tiene el return, podemos borrar las llaves y el return de la funcion
    const sumar = ( a, b ) => a + b;

    const saludar = () => `Hola Mundo`;

    console.log( sumar( 5, 10 ) );
    console.log( saludar() );